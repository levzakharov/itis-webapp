<div class="panel">
    <div class="days">

        <div class="button <#if day_of_week == "MONDAY"> active</#if>">Пн</div>
        <div class="button <#if day_of_week == "TUESDAY"> active</#if>">Вт</div>
        <div class="button <#if day_of_week == "WEDNESDAY"> active</#if>">Ср</div>
        <div class="button <#if day_of_week == "THURSDAY"> active</#if>">Чт</div>
        <div class="button <#if day_of_week == "FRIDAY"> active</#if>">Пт</div>
        <div class="button <#if day_of_week == "SATURDAY"> active</#if>">Сб</div>
        <div class="button <#if day_of_week == "SUNDAY"> active</#if>">Вс</div>
    </div>
</div>